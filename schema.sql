CREATE TABLE posts (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    title TEXT NOT NULL,
    act TEXT NOT NULL,
    animal TEXT NOT NULL,
    blurb TEXT NOT NULL,
    usstate TEXT NOT NULL,
    months TEXT NOT NULL
);